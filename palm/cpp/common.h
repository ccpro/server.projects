#include <stdio.h>
#include <iostream>
#include <fstream>
#include <string>
#include <vector>
using namespace std;

#include "const.h"

int skip_new_line(string &str);
string read_string(ifstream &ifs);
