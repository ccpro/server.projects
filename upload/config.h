#ifndef __CONFIG_H__
#define __CONFIG_H__

#define STRLEN 80

#define NOT_ENTERED(s) ((s == NULL) || (s[0] == '\0') || (s[0] == '0'))
#define NULL_OR_EMPTY(s) ((s == NULL) || (s[0] == '\0'))

#endif // __CONFIG_H__
